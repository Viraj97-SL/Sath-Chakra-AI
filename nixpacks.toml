[phases.setup]
nixPkgs = ["python311", "playwright-driver", "nodejs"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "playwright install --with-deps chromium"
]

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"